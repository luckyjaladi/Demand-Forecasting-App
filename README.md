# Demand-Forecasting-App
A machine learning-based demand forecasting application that uses XGBoost and time-series features (lag values, rolling means, seasonality) to predict future sales. The model is deployed via Streamlit, allowing users to input parameters and get real-time demand predictions with visual insights.
